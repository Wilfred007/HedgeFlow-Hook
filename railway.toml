[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/risk-engine/Dockerfile"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
